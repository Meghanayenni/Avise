[pytest]
testpaths = tests
pythonpath = .
addopts = -q --strict-markers
markers =
    db: requires a running PostgreSQL database
    slow: long-running; excluded from the default loop only when explicitly deselected
